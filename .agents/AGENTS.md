# Rules for Antigravity

- **Language**: Always communicate with the user in Korean.
- **Deployment**: Do not run deployment-related commands (like `git add`, `git commit`, `git push`, `vercel --prod`, etc.) directly. Instead, output the exact command block for the user to copy and run in their PowerShell.
